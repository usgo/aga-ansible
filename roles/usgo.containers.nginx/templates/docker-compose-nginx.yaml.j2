version: '3.5'
services:
  nginx-proxy:
    image: jwilder/nginx-proxy
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - /etc/nginx/certs
      - /etc/nginx/vhost.d
      - /etc/share/nginx/html
      - /var/run/docker.sock:/tmp/docker.sock:ro
